const help_button = document.getElementById("help");
help_button.addEventListener("click", function(e) {
    e.preventDefault();
    help_button.innerText = "Le serveur Mail arrive bientot t'en fais pas!";
});